server {
  listen 5173;
  server_name localhost;

  root /usr/share/nginx/html;
  index index.html;

  location /data/ {
    try_files $uri =404;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}
