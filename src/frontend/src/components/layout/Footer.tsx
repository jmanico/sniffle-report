export function Footer() {
  return (
    <footer className="site-footer">
      <div className="site-footer__panel">
        <span>Sniffle Report is structured around region-scoped public health context.</span>
        <span>Public routes are in place; live API wiring follows in the next frontend issues.</span>
      </div>
    </footer>
  )
}
